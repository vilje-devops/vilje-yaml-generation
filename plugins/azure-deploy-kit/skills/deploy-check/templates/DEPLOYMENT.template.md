<!--
DEPLOYMENT.md template. Fill every {{TOKEN}} from detector output, user answers, the files
you generated, and validator output. Delete rows and blocks that do not apply, and delete
this comment.

RULES
- Never invent a value. Unknown -> {{NOT PROVIDED}} plus a section 3 checklist item.
- Secret NAMES only. Never a value, never publish profile XML.
- Never say the deployment succeeded - it has not run.
- Keep the section 5 disclaimer word for word.
- Label facts [Verified] (you saw it) / [Assumed] (inferred) / [Unknown] (needs Azure or
  GitHub access you do not have). Azure state is ALWAYS Unknown.
- Plain language: a non-technical reader should follow it. Explain each term once.
- Be brief. Aim for 120 lines. Cut any row that does not change what the reader does.
- Troubleshooting rows must match THIS app. Generic advice is filler - delete it.
Then run: scripts/validate_report.py DEPLOYMENT.md
-->
# Deployment guide — {{APP_NAME}}

**Nothing has been deployed.** This guide and the workflow files were generated by
analyzing this repository. A person still has to create the Azure resources, add the
secrets, and push.

| | |
|---|---|
| **Status** | {{STATUS}} |
| **Hosting** | {{AZURE_TARGET_FRIENDLY}} |
| **Sign-in method** | {{AUTH_FRIENDLY}} |
| **Deploys when** | {{TRIGGER_FRIENDLY}} |
| **Generated** | {{DATE}} by azure-deploy-kit {{PLUGIN_VERSION}} |

---

## 1. What did the skill create?

| File | What it does | New or changed |
|---|---|---|
| {{FILE_PATH}} | {{FILE_PURPOSE}} | {{FILE_STATUS}} |

**Detected:** {{LANGUAGE}} / {{FRAMEWORK}}, {{PACKAGE_MANAGER}}, runtime {{RUNTIME_VERSION}}
(pinned in {{VERSION_SOURCE}}), build `{{BUILD_COMMAND}}`, start `{{START_COMMAND}}`,
port {{PORT}}, containerized {{CONTAINERIZED}}. [{{LABEL}}]

**Problems found: {{FINDINGS_SUMMARY}}**

<!-- One block per blocker and warning. Skip entirely if none. -->
**{{CHECK_ID}}** · {{SEVERITY}} · [{{LABEL}}] — {{WHAT_IS_WRONG}} (`{{FILE}}:{{LINE}}`)
{{WHY_IT_MATTERS}} **Fix:** {{HOW_TO_FIX}}

*Not applicable here: {{SKIPPED_CHECKS}}.*

---

## 2. How does the deployment work?

{{ONE_SENTENCE_SUMMARY}}

1. {{STEP_1}}
2. {{STEP_2}}
3. {{STEP_3}}
4. {{STEP_4}}
5. {{STEP_5}}

{{APPROVAL_NOTE}}

**If any step fails the run stops** — a failed build never reaches Azure.

---

## 3. What do I need to configure?

One-time setup, done by a person.

### GitHub secrets

Repository → **Settings** → **Secrets and variables** → **Actions** → **New repository secret**.

> Secret values never appear in this document and must never be pasted into chat.

| Secret name | What to put in it | Where to get it |
|---|---|---|
| {{SECRET_NAME}} | {{SECRET_DESCRIPTION}} | {{SECRET_SOURCE}} |

<!-- GitHub variables (${{ vars.X }}) - delete this block if none. Not secrets, but they
     must exist: Settings > Secrets and variables > Actions > Variables. -->
| Variable name | Value | Purpose |
|---|---|---|
| {{VARIABLE_NAME}} | {{VARIABLE_VALUE}} | {{VARIABLE_PURPOSE}} |

### Azure resources

**[Unknown]** — the skill cannot see your Azure account and cannot check these exist.

| Resource | Name | Notes |
|---|---|---|
| {{RESOURCE_TYPE}} | {{RESOURCE_NAME}} | {{RESOURCE_NOTES}} |

{{AUTH_SETUP_STEPS}}
<!-- Chosen method only. OIDC: app registration, the EXACT federated credential subject,
     role assignment and scope. SP secret: `az ad sp create-for-rbac --sdk-auth`, note the
     expiry. Publish profile: portal > Web App > Get publish profile, paste into the secret
     above, re-download if credentials are reset, and set the Startup Command in
     Configuration > General settings (the workflow cannot set it). -->

### Before the first push

- [ ] {{BLOCKER_FIX}}
- [ ] Azure resource `{{AZURE_APP_NAME}}` exists and is running
- [ ] Every secret above has been added
- [ ] App settings set on the Azure resource: {{REQUIRED_ENV_VARS}}
- [ ] Someone has read `{{DEPLOY_WORKFLOW_PATH}}` and agrees with it

---

## 4. How do I run the deployment?

{{RUN_INSTRUCTIONS}}
<!-- Concrete: finish the checklist, commit, `git push origin <branch>`, then open the
     Actions tab. For workflow_dispatch describe the Run workflow button. -->

**No Azure commands needed.** The workflow does that; section 3 is one-time.

---

## 5. How do I know whether it succeeded?

1. Open the **Actions** tab of this repository.
2. Click the latest run of **{{WORKFLOW_DISPLAY_NAME}}**.
3. Green tick = every step passed. Red cross = click the failed step for the log.
4. Then open {{APP_URL}}.

{{HEALTH_CHECK_NOTE}}

### Already checked

| Check | Result |
|---|---|
| {{VALIDATION_CHECK}} | {{PASSED_FAILED}} |

**Not performed:** {{UNCHECKED_LIST}}
<!-- Each with its reason: actionlint not installed, no Azure access, --run-build not set. -->

> YAML validation confirms syntax and structure only. It does not prove the deployment
> will succeed. Azure resources, permissions, quotas and network paths are unverified.

### If it fails

| Symptom | Likely cause | What to do |
|---|---|---|
| {{SYMPTOM}} | {{CAUSE}} | {{REMEDY}} |

Re-check anytime with `/deploy-check validate`.

---

*Generated by azure-deploy-kit {{PLUGIN_VERSION}} on {{DATE}}. This tool writes and checks
files; it does not deploy.*
