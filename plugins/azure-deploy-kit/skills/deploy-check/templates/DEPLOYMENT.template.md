<!--
Template for DEPLOYMENT.md. Fill every {{TOKEN}} from the detector output, the user's
confirmed answers, the files you generated, and the validator output. Delete rows and
whole blocks that do not apply to this repository.

Rules (full version in ../references/report-format.md):
  - Never invent a value. Unknown becomes {{NOT PROVIDED}} plus a checklist item.
  - Secret NAMES only. Never a secret value, never publish profile XML.
  - Never claim the deployment succeeded - it has not been run.
  - Keep the disclaimer in section 5 word for word.
  - Label facts [Verified] / [Assumed] / [Unknown].
  - Delete this comment block from the generated file.
-->
# Deployment guide — {{APP_NAME}}

**Nothing has been deployed.** This guide and the workflow files were generated by
analyzing this repository. A person still has to create the Azure resources, add the
secrets, and push the code. Steps for all of that are below.

| | |
|---|---|
| **Status** | {{STATUS}} <!-- Ready to deploy / Ready with caveats / Blocked - N blockers --> |
| **Hosting** | {{AZURE_TARGET_FRIENDLY}} <!-- e.g. Azure App Service (runs your code directly, no container) --> |
| **Sign-in method** | {{AUTH_FRIENDLY}} <!-- e.g. Publish profile - a file downloaded from Azure that lets GitHub deploy --> |
| **Deploys when** | {{TRIGGER_FRIENDLY}} <!-- e.g. someone pushes to the `main` branch --> |
| **Generated** | {{DATE}} by azure-deploy-kit {{PLUGIN_VERSION}} |

---

## 1. What did the skill create?

### Files written

| File | What it does | New or changed |
|---|---|---|
| {{FILE_PATH}} | {{FILE_PURPOSE}} | {{FILE_STATUS}} |

<!-- If any file was written as *.generated.yml because one already existed, say so here
     and describe what differs. Never overwrite silently. -->

### What was found in this repository

| | | |
|---|---|---|
| Language | {{LANGUAGE}} | [{{LABEL}}] |
| Framework | {{FRAMEWORK}} | [{{LABEL}}] |
| Package manager | {{PACKAGE_MANAGER}} | [{{LABEL}}] |
| Runtime version | {{RUNTIME_VERSION}}, pinned in {{VERSION_SOURCE}} | [{{LABEL}}] |
| Build command | `{{BUILD_COMMAND}}` | [{{LABEL}}] |
| Start command | `{{START_COMMAND}}` | [{{LABEL}}] |
| Listens on port | {{PORT}} | [{{LABEL}}] |
| Containerized | {{CONTAINERIZED}} | [{{LABEL}}] |

### Problems found

{{FINDINGS_SUMMARY}} <!-- e.g. "2 blockers, 3 warnings" or "No blockers." -->

<!-- One block per finding, blockers first. Delete the heading if there are none. -->
**{{CHECK_ID}}** · {{SEVERITY}} · [{{LABEL}}]
{{WHAT_IS_WRONG}} (`{{FILE}}:{{LINE}}`)
{{WHY_IT_MATTERS}}
**Fix:** {{HOW_TO_FIX}}

*Checks not applicable here: {{SKIPPED_CHECKS}}.*

---

## 2. How does the deployment work?

In plain terms: {{ONE_SENTENCE_SUMMARY}}
<!-- e.g. "When you push to main, GitHub builds your app, then copies the result to Azure." -->

Step by step, each time it runs:

1. {{STEP_1}} <!-- e.g. "You push code to the `main` branch." -->
2. {{STEP_2}} <!-- e.g. "GitHub Actions starts the workflow in .github/workflows/deploy.yml" -->
3. {{STEP_3}} <!-- e.g. "It installs dependencies with `npm ci`." -->
4. {{STEP_4}} <!-- e.g. "It runs the tests. If any fail, everything stops here and nothing is deployed." -->
5. {{STEP_5}} <!-- e.g. "It builds the app with `npm run build`." -->
6. {{STEP_6}} <!-- e.g. "It signs in to Azure using the publish profile secret." -->
7. {{STEP_7}} <!-- e.g. "It uploads the built files to the App Service named vilje-api-prod." -->
8. {{STEP_8}} <!-- e.g. "It calls your /health endpoint until it answers OK, so a broken deploy is not reported as success." -->

{{APPROVAL_NOTE}}
<!-- If a GitHub Environment gate exists: "Before step 7, the run pauses until someone
     listed as a reviewer on the `production` environment approves it." Otherwise delete. -->

**If any step fails, the run stops and later steps do not happen.** A failed build never
reaches Azure.

---

## 3. What do I need to configure?

Everything here is done by a person, once, before the first deployment.

### GitHub secrets

Add these at: your repository → **Settings** → **Secrets and variables** → **Actions** →
**New repository secret**.

> Secret **values** are never shown in this document and must never be pasted into chat or
> committed to the repository.

| Secret name | What to put in it | Where to get it |
|---|---|---|
| {{SECRET_NAME}} | {{SECRET_DESCRIPTION}} | {{SECRET_SOURCE}} |

### GitHub variables

<!-- Delete this whole subsection if there are none. -->

| Variable name | Value | Purpose |
|---|---|---|
| {{VARIABLE_NAME}} | {{VARIABLE_VALUE}} | {{VARIABLE_PURPOSE}} |

### GitHub environment

{{ENVIRONMENT_SETUP}}
<!-- e.g. "Create an environment named `production` (Settings > Environments) and add
     <names> as required reviewers." Or delete if no environment is used. -->

### Azure resources

These must exist before the first run. **[Unknown]** — the skill has no access to your
Azure account and cannot check whether any of them already exist.

| Resource | Name to use | Notes |
|---|---|---|
| {{RESOURCE_TYPE}} | {{RESOURCE_NAME}} | {{RESOURCE_NOTES}} |

{{AUTH_SETUP_STEPS}}
<!-- Auth-specific setup, from ../references/auth.md. Keep only the chosen method:
     OIDC            - the app registration, the EXACT federated credential subject
                       string, and the role assignment and its scope.
     SP secret       - `az ad sp create-for-rbac --sdk-auth`, and note the expiry date.
     Publish profile - Azure portal > the Web App > Get publish profile (downloads a
                       file), then paste its contents into the secret named above.
                       It must be downloaded again if the publishing credentials are
                       ever reset. -->

### Pre-deployment checklist

Tick every box before the first push.

- [ ] {{BLOCKER_FIX}} <!-- one line per blocker from section 1; delete if none -->
- [ ] Azure resource `{{AZURE_APP_NAME}}` exists and is running
- [ ] Every GitHub secret listed above has been added
- [ ] {{ENVIRONMENT_CHECKLIST_ITEM}} <!-- delete if no environment -->
- [ ] Application settings / environment variables are set on the Azure resource: {{REQUIRED_ENV_VARS}}
- [ ] {{PORT_CHECKLIST_ITEM}} <!-- e.g. "The app reads the PORT environment variable" -->
- [ ] Someone has read `{{DEPLOY_WORKFLOW_PATH}}` and agrees with what it does

---

## 4. How do I run the deployment?

{{RUN_INSTRUCTIONS}}
<!-- Concrete and specific to the configured trigger. For a push trigger:

     1. Finish the checklist in section 3.
     2. Commit your changes.
     3. Push to the `main` branch:
            git push origin main
     4. Open the **Actions** tab of the repository on GitHub. A run named
        "Deploy to Azure App Service" appears within a few seconds.

     For workflow_dispatch, describe the Run workflow button instead. -->

{{MANUAL_RUN_NOTE}}
<!-- If workflow_dispatch is also enabled: "You can also start it by hand from the
     Actions tab: pick the workflow, then Run workflow." Otherwise delete. -->

**No one needs to run any Azure command to deploy.** The workflow does that. The Azure
setup in section 3 is one-time.

---

## 5. How do I know whether it succeeded?

### Where to look

1. Open the **Actions** tab of this repository on GitHub.
2. Click the most recent run of **{{WORKFLOW_DISPLAY_NAME}}**.
3. A green tick means every step passed, including the health check in step
   {{HEALTH_STEP_NUMBER}}. A red cross means it stopped — click the failed step to read
   the log.
4. When it is green, open {{APP_URL}} to see the running application.

{{HEALTH_CHECK_NOTE}}
<!-- e.g. "The workflow calls {{HEALTH_PATH}} up to 10 times after deploying. If the app
     never answers, the run is marked failed even though the files were copied - so a
     green run means the app actually responded." Delete if no health check step. -->

### What this skill already checked

| Check | Result | Detail |
|---|---|---|
| {{VALIDATION_CHECK}} | {{PASSED_FAILED}} | {{VALIDATION_DETAIL}} |

**Not performed** — these were not checked, and why:

| Not checked | Reason |
|---|---|
| {{UNCHECKED_ITEM}} | {{UNCHECKED_REASON}} |

> YAML validation confirms syntax and structure only. It does not prove the deployment
> will succeed. Azure resources, permissions, quotas and network paths are unverified.

### If it fails — common causes

<!-- Keep only rows that apply to THIS app. Delete the rest. -->

| Symptom | Likely cause | What to do |
|---|---|---|
| {{SYMPTOM}} | {{CAUSE}} | {{REMEDY}} |

### Still stuck?

Re-run the analysis to see whether anything drifted:

```
/deploy-check validate
```

---

*Generated by azure-deploy-kit {{PLUGIN_VERSION}} on {{DATE}}. This tool writes and checks
files; it does not deploy. Re-run `/deploy-check` after changing the application's build,
runtime or hosting.*
